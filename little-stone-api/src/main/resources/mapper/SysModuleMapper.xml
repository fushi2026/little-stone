<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fushi.mapper.SysModuleMapper">
    <select id="selectModulesByUserId" resultType="com.fushi.entity.SysModule">
        SELECT
            m.id,
            m.module_code,
            m.module_name,
            m.sort,
            m.icon,
            m.status,
            m.create_time,
            m.update_time,
            m.remark
        FROM sys_module m
            JOIN sys_role_module rm ON rm.module_code = m.module_code
            JOIN sys_user_role ur ON ur.role_id = rm.role_id
        WHERE ur.user_id = #{userId}
            AND m.status = 1
    </select>
</mapper>